<!DOCTYPE html>
<html>
<head>
    <title>{{ page.title }} - {{ settings.siteName }}</title>
    <meta name="description" content="{{ page.metaDescription ?? settings.seoDescription }}">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1>{{ page.title }}</h1>
        <div class="content">{{ page.content|raw }}</div>
        <hr>
        <p class="text-muted">Views: {{ page.viewCount }} | Published: {{ page.publishedAt|date('Y-m-d') }}</p>
    </div>
</body>
</html>
