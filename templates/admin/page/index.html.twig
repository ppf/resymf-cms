{% extends 'admin/base.html.twig' %}

{% block title %}Pages{% endblock %}

{% block body %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>Pages</h1>
    <a href="{{ path('admin_page_new') }}" class="btn btn-primary">Create New Page</a>
</div>

<div 
    data-vue-island="page-grid"
    data-api-url="{{ path('api_pages_list') }}"
    data-csrf-token="{{ csrf_token('toggle') }}"
></div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ vite_entry_script_tags('page-grid-app') }}
{% endblock %}
