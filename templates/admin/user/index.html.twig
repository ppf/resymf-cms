{% extends 'admin/base.html.twig' %}

{% block title %}Users{% endblock %}

{% block body %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1 class="mb-0">Users Management</h1>
        <p class="text-muted">Manage user accounts and permissions</p>
    </div>
    <a href="{{ path('admin_user_new') }}" class="btn btn-primary">
        <i class="bi bi-plus-circle me-2"></i>New User
    </a>
</div>

{# Vue Island: Customers Grid #}
<div data-vue-island="customers-grid"></div>

{{ vite_entry_script_tags('admin_users_vue') }}
{% endblock %}
